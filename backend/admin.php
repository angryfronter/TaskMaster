<?php
session_start();

// Проверяем, авторизован ли пользователь как администратор
if ($_SESSION['role'] !== 'admin') {
    // Если пользователь не является администратором, перенаправляем его на другую страницу или выводим сообщение об ошибке
    header('Location: /error.php');
    exit;
}

// Дальше идет ваш код для страницы "admin.php"

// Пример: Вывод списка пользователей
$users = []; // Здесь должен быть ваш код для получения списка пользователей из базы данных

foreach ($users as $user) {
    echo $user['name'] . '<br>';
    // Вывод дополнительной информации о каждом пользователе
    // ...
}

?>
